随着大语言模型在关键决策领域的应用，其生成内容的事实准确性已成为衡量可信度的核心标尺。本文系统地剖析了**事实性保障的两大核心支柱：推理阶段的置信度校准、训练阶段的事实性内化**。前者聚焦于模型如何通过分析Logits分布、语义熵和不确定性估计实现自我监督；后者探讨如何通过强化学习、知识编辑和检索增强等技术，将外部事实约束编码为模型的内在参数。

本文结合Google Gemini工业实践，引入算法描述与代码解析，旨在为大模型的可靠部署提供从理论到实践的透视。

# 一、引言：事实性问题与模型的内在矛盾

大语言模型在展现出惊人“知识”的同时，也暴露了其“幻觉”生成的本质缺陷。这种缺陷根植于模型的自回归生成范式：模型在训练中被优化为基于上下文预测下一个最可能的Token，其目标函数驱动它生成“流畅”而非“正确”的文本。当面临知识边界时，模型倾向于依据训练语料中的统计模式进行“合理编造”，而非承认无知。因此，构建事实性保障机制的核心，在于重塑模型的决策逻辑，使其在生成流暢文本与遵循事实真相之间取得平衡。

根据Google Gemini技术报告所披露的内容，Gemini的“事实性保障机制”核心职责是保障Gemini面向C端用户的输出事实准确性。这一机制并非简单的事后过滤，而是从模型训练和强化学习的源头入手，通过设计专门的奖励函数，让模型在生成每一个Token的时候，就内化了“事实准确性”这个约束。在推理层面，这体现为模型内置的“事实性感知”解码逻辑：当Gemini模型在处理用户查询时，其内部的奖励模型（或类似机制）会对潜在的生成长路径进行快速评估，抑制那些可能导致低事实性得分的路径分支。这本质上是一种基于前瞻搜索的约束解码，将训练阶段学到的事实性偏好，实时应用于推理时的路径选择。

# 二、推理阶段：基于Logits分布的实时诊断与干预

在模型逐词生成的过程中，其内部的Logits向量是窥探模型“思考过程”的关键窗口。Logits是Softmax函数前的原始输出分数，代表了模型对词表中每个候选Token的“原始偏好”。事实性保障在推理阶段的首要任务，就是解读这些Logits信号，判断模型是否在“瞎编”。

## 2.1、Logits分布与不确定性度量

对于一个给定的上下文，模型输出的Logits向量经过Softmax后转换为概率分布。一个“确定”的事实性回答，其概率分布通常表现出尖锐的峰值——正确Token的概率远高于其他Token。反之，当模型“不确定”或“编造”时，概率分布往往更加平坦。

- **核心算法：基于熵的置信度评分**

信息熵是度量概率分布不确定性的经典指标。我们可以通过计算模型在生成关键事实Token（如命名实体、数字、日期）时的Token级熵，来实时评估其置信度。

import torch

import torch.nn.functional as F

def compute_token_confidence(logits):

"""

计算基于Logits分布的单Token生成置信度。

Args:

logits: 形状为 \[vocab_size\] 的模型原始输出。

Returns:

max_prob: 最大概率值（置信度）。

entropy: 概率分布的信息熵。

"""

probs = F.softmax(logits, dim=-1)

max_prob, _ = torch.max(probs, dim=-1) # 最高概率值

entropy = -torch.sum(probs \* torch.log(probs + 1e-9), dim=-1) # 信息熵

return max_prob.item(), entropy.item()

\# 应用示例：在生成过程中监控

def monitor_generation_confidence(model, tokenizer, prompt, max_length=50):

input_ids = tokenizer.encode(prompt, return_tensors='pt')

generated_ids = \[\]

confidence_history = \[\]

for _ in range(max_length):

outputs = model(input_ids)

next_token_logits = outputs.logits\[:, -1, :\] # 获取下一个词的Logits

\# 计算置信度

confidence, entropy = compute_token_confidence(next_token_logits.squeeze())

confidence_history.append((confidence, entropy))

\# 决策：如果熵过高，可触发拒绝或降级处理

if entropy > 3.0: # 示例阈值

\# 选择“不确定”的Token，而非概率最高的Token

next_token_id = tokenizer.encode(" \[不确定\]")\[0\]

else:

\# 常规贪婪解码

next_token_id = torch.argmax(next_token_logits, dim=-1).item()

generated_ids.append(next_token_id)

input_ids = torch.cat(\[input_ids, torch.tensor(\[\[next_token_id\]\])\], dim=-1)

if next_token_id == tokenizer.eos_token_id:

break

return tokenizer.decode(generated_ids), confidence_history

- **算法解析**

上述代码实现了在生成流中实时监控置信度的机制。compute_token_confidence函数计算了模型在每一步的“把握程度”。monitor_generation_confidence函数则在解码循环中，当检测到熵值过高（模型混乱）时，主动注入\[不确定\]这样的元词，而非硬着头皮生成一个高概率但可能错误的词。这种方法直接干预了生成过程，是推理阶段事实性保障的“主动刹车”。

## 2.2、语义层面的一致性验证：超越Token熵

Token级熵可能无法捕捉跨多个Token的语义矛盾。例如，模型可能以高置信度逐个生成“唐朝”、“开国皇帝”、“是”、“李世民”，尽管整体陈述是错误的。为此，需要引入语义级验证。

- **假设-验证循环**：模型首先生成一个候选答案，然后将其作为一个新的假设性问题输入给自己或一个验证器模型，要求其判断真伪。例如，生成“李世民是唐朝开国皇帝”后，立即提问“唐朝的开国皇帝是李世民吗？”，通过二次回答的一致性进行验证。
- **自洽性检查**：对于需要多步推理的问题，要求模型生成推理链（Chain-of-Thought），然后检查链中每一步的前提和结论是否自洽。任何逻辑断裂都可能指向事实性错误。

# 三、训练阶段：重塑Logits分布的事实性内化

推理阶段的干预治标，训练阶段的内化才能治本。目标是调整模型的数百万甚至数千亿参数，使得在面对事实性问题时，**正确的答案在Logits空间中自然获得更高的激活**，而幻觉答案则被抑制。

RLHF是当前实现这一目标的核心技术。其过程分为三步，每一步都深刻影响着模型的Logits分布：

- **监督微调**：在高质量的指令遵循数据上微调，让模型初步学会遵循人类提问格式，但这对其事实性提升有限。
- **奖励模型训练**：这是关键一步。标注员对同一提示词的不同模型输出进行排序（如A比B更好）。奖励模型被训练来预测这种偏好，其学习目标是最小化以下对比损失：

奖励模型最终学会将“事实正确”、“有用”、“无害”等抽象属性映射为一个标量分数。

- **强化学习优化**：使用PPO算法优化策略模型（即我们要改进的大模型）。目标函数为：

在PPO的每一步，策略模型生成回答，奖励模型给出分数。梯度回传通过策略模型，调整其参数θ，使得未来生成能获得高奖励的回答的概率（即对应Token的Logits）增加。KL散度项则确保模型的输出分布不会变得过于“怪异”，保持语言的基本通顺性。经过数百万次这样的“尝试-评分-调整”循环，模型逐渐将其Logits分布与人类的事实性偏好对齐。面对“亚特兰蒂斯首都在哪”这种问题，生成“这是传说，无实证”的Logits会升高，而生成“波塞多尼亚”的Logits会受到抑制。

# 四、前沿与展望：内化与外显的融合

未来的事实性保障机制，将更加注重推理与训练的协同，以及模型自我监督能力的提升。

- **训练-推理一致性**：未来的训练目标会直接纳入推理时的不确定性估计能力。例如，在训练中就让模型学会输出其答案的置信度区间，并在RLHF阶段奖励“正确且校准良好”（即高置信度对应高正确率）的行为。
- **自我检索与验证**：模型将不再是被动接受RAG提供的文档，而是能主动规划检索请求，迭代地检索、综合、验证信息，直到形成一个高置信度的答案。这接近于将周浩在Gemini中负责的多步骤强化学习能力与RAG深度结合。
- **可解释的Logits**：通过分析模型内部注意力机制、激活值在事实正确与错误生成时的差异，构建可解释的事实性归因工具。这不仅能帮助检测幻觉，还能指导更高效的知识编辑和模型诊断。